using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoorManager : MonoBehaviour, ISwitchable
{
    public void Toggle(bool toggleStatus)
    {
        
    }
    private void UpdateDoorState(bool toggle)
    {
        //triggers toggle if open or close the doors
    }
}
